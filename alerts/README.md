# Alerts (mirror)

Canonical Prometheus rules loaded by the stack:

`../config/prometheus/rules/`

Files here are mirrored copies for plan layout / review.  
Edit the canonical path, then sync here (or copy both ways) and update `brain/`.
